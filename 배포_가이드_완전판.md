# 🚀 매니아그룹 웹사이트 배포 가이드

GitHub + Vercel + Supabase 통합 배포

---

## 📋 필요한 것

- GitHub 계정
- Vercel 계정
- Supabase 계정
- Git 설치됨

---

# 1단계: GitHub에 코드 업로드

## 1-1. GitHub 저장소 생성

1. https://github.com 접속
2. 로그인
3. 우측 상단 **+** 클릭 → **New repository**
4. 저장소 이름: `maniagroup-web`
5. **Private** 선택 (비공개)
6. **Create repository** 클릭

## 1-2. 코드 업로드

**Windows PowerShell에서:**

```powershell
cd "C:\Users\user\OneDrive\민기현_SUPERS\매니아그룹\maniagroup-web"

# Git 사용자 설정 (최초 1회만)
git config user.name "Your Name"
git config user.email "your-email@example.com"

# 원격 저장소 연결
git remote add origin https://github.com/your-username/maniagroup-web.git

# 푸시
git push -u origin main
```

**GitHub 인증 요청 나오면:**
- 사용자명 입력
- Personal Access Token 입력 (비밀번호 대신)

---

# 2단계: Supabase 데이터베이스 설정

## 2-1. Supabase 프로젝트 생성

1. https://supabase.com 접속
2. **Start your project** 클릭
3. **New Project** 클릭
4. 프로젝트 설정:
   - Name: `maniagroup`
   - Database Password: 안전한 비밀번호 입력 (저장!)
   - Region: **Northeast Asia (Seoul)**
5. **Create new project** 클릭 (2-3분 소요)

## 2-2. 테이블 생성

**좌측 메뉴 → Table Editor → New Table**

### 테이블 1: partners (파트너)
```sql
CREATE TABLE partners (
  id BIGSERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  category TEXT,
  description TEXT,
  rating DECIMAL,
  projects INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 테이블 2: projects (프로젝트)
```sql
CREATE TABLE projects (
  id BIGSERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  client TEXT,
  status TEXT,
  start_date DATE,
  end_date DATE,
  budget TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 테이블 3: products (제품)
```sql
CREATE TABLE products (
  id BIGSERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  brand TEXT,
  category TEXT,
  supplier TEXT,
  price TEXT,
  status TEXT,
  image TEXT,
  can_group BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 테이블 4: influencers (인플루언서)
```sql
CREATE TABLE influencers (
  id BIGSERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  real_name TEXT,
  platform TEXT,
  category TEXT,
  followers TEXT,
  engagement TEXT,
  instagram TEXT,
  agency TEXT,
  is_exclusive BOOLEAN DEFAULT false,
  contact_method TEXT,
  rating DECIMAL,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 테이블 5: files (파일)
```sql
CREATE TABLE files (
  id BIGSERIAL PRIMARY KEY,
  filename TEXT NOT NULL,
  path TEXT NOT NULL,
  category TEXT,
  size BIGINT,
  uploaded_at TIMESTAMP DEFAULT NOW()
);
```

## 2-3. API 키 확인

**좌측 메뉴 → Project Settings → API**

다음 정보 복사해두기:
- **Project URL**: `https://xxxxx.supabase.co`
- **anon public key**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

---

# 3단계: Vercel 배포

## 3-1. Vercel에서 GitHub 연동

1. https://vercel.com 접속
2. **Continue with GitHub** 클릭
3. GitHub 계정으로 로그인
4. **Import Project** 클릭
5. **Import Git Repository** 선택
6. `maniagroup-web` 저장소 선택
7. **Import** 클릭

## 3-2. 프로젝트 설정

**Build & Development Settings:**
- Framework Preset: **Next.js**
- Build Command: `npm run build`
- Output Directory: `.next`
- Install Command: `npm install`

**Root Directory:** `.` (그대로)

## 3-3. 환경 변수 설정

**Environment Variables 섹션에서 추가:**

```
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Telegram Bot (선택)
TELEGRAM_BOT_TOKEN=8211049535:AAFy28gUJlRBvLX4E152OW2ZjYCz24nZBms
TELEGRAM_GROUP_CHAT_ID=-5107079263

# App
NEXT_PUBLIC_API_URL=https://your-app.vercel.app
```

**Apply to:** All (Production, Preview, Development)

## 3-4. 배포 시작

**Deploy** 클릭!

자동으로:
1. GitHub에서 코드 가져오기
2. npm install
3. npm run build
4. 배포 완료!

---

# 4단계: 배포 확인

## 배포 완료되면

```
✅ https://maniagroup-web.vercel.app
```

URL이 나옵니다!

## 접속 테스트

- **홈페이지**: https://your-app.vercel.app
- **관리자**: https://your-app.vercel.app/admin/login
- **서비스**: https://your-app.vercel.app/services

---

# 5단계: 데이터 마이그레이션 (선택)

## 기존 JSON 데이터를 Supabase로 이전

**Supabase → SQL Editor → New query**

```sql
-- partners 데이터 삽입 예시
INSERT INTO partners (name, category, description, rating, projects)
VALUES
  ('THE8MANIA', '영상 제작', '프리미엄 영상 제작 전문', 4.9, 12),
  ('저스트미라클', '이벤트 대행', '온오프라인 이벤트 기획', 4.7, 8);

-- projects 데이터 삽입 예시
INSERT INTO projects (name, client, status, start_date, end_date, budget)
VALUES
  ('비티진 온라인마케팅', '비티진', '완료', '2022-01-01', '2022-03-31', '₩5,000,000');

-- 계속 추가...
```

---

# 🔄 자동 배포 설정

## GitHub에 푸시하면 자동 배포

```powershell
# 코드 수정 후
git add .
git commit -m "Update features"
git push
```

→ Vercel이 자동으로 감지하고 새 버전 배포!

---

# 🔧 유용한 명령어

## 로컬 개발

```powershell
npm run dev
```

→ http://localhost:3000

## 빌드 테스트

```powershell
npm run build
npm start
```

## 환경 변수 로컬 테스트

`.env.local` 파일 생성:
```
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbG...
```

---

# 📊 배포 상태 확인

## Vercel 대시보드

- **Deployments**: 배포 기록
- **Analytics**: 방문자 통계
- **Logs**: 에러 로그

## Supabase 대시보드

- **Table Editor**: 데이터 확인/수정
- **Authentication**: 사용자 관리
- **Storage**: 파일 업로드 (선택)

---

# 🆘 문제 해결

## 배포 실패

**Vercel → Deployments → 실패한 배포 클릭 → Logs**

에러 메시지 확인

## 환경 변수 오류

**Vercel → Settings → Environment Variables**

값이 정확한지 확인 후 재배포

## 데이터베이스 연결 안됨

**Supabase URL과 Key 확인:**
- Supabase → Settings → API
- Vercel 환경 변수와 일치하는지 확인

## GitHub 푸시 안됨

```powershell
git remote -v  # 원격 저장소 확인
git pull origin main  # 최신 코드 받기
git push  # 다시 푸시
```

---

# 📱 사용자 정의 도메인 (선택)

## Vercel에서 도메인 연결

1. Vercel → Settings → Domains
2. **Add Domain** 클릭
3. 도메인 입력 (예: maniagroup.com)
4. DNS 설정 안내에 따라 도메인 제공업체에서 설정

---

# ✅ 체크리스트

- [ ] GitHub 저장소 생성 완료
- [ ] 코드 푸시 완료
- [ ] Supabase 프로젝트 생성 완료
- [ ] 테이블 생성 완료
- [ ] Vercel 프로젝트 생성 완료
- [ ] 환경 변수 설정 완료
- [ ] 배포 성공
- [ ] URL 접속 확인
- [ ] 관리자 페이지 작동 확인
- [ ] 데이터 확인

---

# 🎉 완료!

**배포 URL을 팀원들과 공유하세요!**

- 홈페이지: https://your-app.vercel.app
- 관리자: https://your-app.vercel.app/admin

**수정사항은 GitHub에 푸시하면 자동 배포됩니다!**
